adicionar, ler e deletar

const myMap = new Map()

myMap.set ('apple', 'fruit');
apple é chave, o valor é fruit

checar o valor de apple myMap.get

deletar myMap.delete

map x objeto
objeto so string
map pode ser nro, etc.
map possui length

exemplo de conjunto.
conjunto a: 1, 2, 3
funcao y =2x
contjunto b: 2, 4, 6
no java: cria um novo array,nao modifica o array inicial.
realiza as operações em ordem.

sintaxe:
array.map9callback, thisArg)
callback(item, index, array) dentro do callback tem acesso à <==
argumento this, pouco usado para mapeamento.

map vc forEach (faz praticamente a mesma coisa)
diferença: valor de retorno,
vai precisar de um array auxliar.
 