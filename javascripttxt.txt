javascript
é uma linguagem interpretada. Há 2 (inter e compiladas).
Ela é rodada em tempo real, e o codigo em tempo real.
Compiladas transforma em outra coisa antes de enviar para o browser (c++).
muito comum em aplicações web client-side (utilizado web sites com interacao com clientes).
5ecmascript: json, string, array
6:declara classes (orientacao a objetos), 
Aplicações: não é só fazer site. Faz mobile, games, Apis, smartwaches. Coisas da internet (rotina para alexa - com node).

Comentar uma linha // (atalho vs 
comentar um paragrafo /*  */

declarando e chamando uma função.
Declarar:
function soma(a, b) {
	return a + b; ((usado para quando é necessário mexer nela depois
}

soma(3, 5);

console.log(a + b ) mostra no console se a função está funcionando. Para debugar.

array = lista

arvores de dependencia DOM.

tagname: é um h1, p, html

eventos: propriedades do javascript.

funções.
1) declare a função
function NOMEDAFUNCAO(parametro) {
	instruções
}